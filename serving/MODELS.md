# Model pool snapshot

Not yet generated — run `make models` (`serving/fetch_models.sh`) once
Ollama is up. This file is overwritten by that script; see doc 01 §6's
licensing-checklist requirement (name, revision, license, quantization, eval
score snapshot, date — recorded here as each checkpoint is actually pulled).
